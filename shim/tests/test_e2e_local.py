# Local e2e smoke (manual) — see plan Task B5.
# 1) shim returns 401 on /mcp/ without a bearer.
# 2) with the bearer, the proxied tool list has 16 tools and excludes
#    checkout / select_slot / get_smart_product_matches.
# Run procedure documented in the plan; not an automated pytest.
